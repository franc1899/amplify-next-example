type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
  nuevo: String
}

type Ejemplo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
} 